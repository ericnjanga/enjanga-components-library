import { mockHeading } from '../mixed';
import { HDG_propsType } from '@/components/Heading/libs/types';

export const headingArgs: Partial<HDG_propsType> = {
  className: '',
  level: 1,
  children: mockHeading.plain,
};
